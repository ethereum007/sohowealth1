import Link from "next/link";
import { ContextualLeadForm } from "@/components/leads/ContextualLeadForm";
import type { LeadIntent } from "@/lib/leads/types";
type Section = { title:string; items:string[] };
export function SampleDeliverablePage({eyebrow,title,intro,sections,intent}:{eyebrow:string;title:string;intro:string;sections:Section[];intent:LeadIntent}){return <main className="pt-20"><article><header className="bg-[#0B1F3A] py-16 text-white"><div className="container mx-auto max-w-4xl px-6"><p className="text-xs font-bold uppercase tracking-[.2em] text-[#E7C96E]">{eyebrow} · illustrative sample</p><h1 className="mt-4 font-display text-4xl font-semibold md:text-5xl">{title}</h1><p className="mt-5 max-w-3xl text-lg text-white/75">{intro}</p></div></header><div className="container mx-auto max-w-4xl px-6 py-14"><aside className="rounded-xl border-l-4 border-[#C9A84C] bg-[#FDF8EC] p-5 text-sm text-slate-700">This open sample uses fictional circumstances and contains no client data. It shows format and decision logic, not a recommendation or promised outcome.</aside><div className="mt-10 space-y-10">{sections.map((section,index)=><section key={section.title}><h2 className="text-2xl font-semibold text-[#0B1F3A]">{index+1}. {section.title}</h2><ul className="mt-4 space-y-3">{section.items.map(item=><li key={item} className="rounded-lg bg-slate-50 p-4 text-slate-700">{item}</li>)}</ul></section>)}</div><div className="mt-12 flex gap-5 text-sm font-semibold"><Link className="underline" href="/why-us">How SoHo Wealth works</Link><Link className="underline" href="/disclosures">Disclosures</Link></div><div className="mt-12 rounded-2xl bg-[#0B1F3A] p-6 md:p-10"><ContextualLeadForm intent={intent} /></div></div></article></main>}
