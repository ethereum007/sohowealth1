import Link from "next/link";
import { getPmsResearchPage, PMS_PERFORMANCE_AS_OF } from "@/lib/pms/research-server";
export function PmsResearchPreview(){ const {records,total}=getPmsResearchPage(); return <section className="bg-slate-50 py-16"><div className="container mx-auto max-w-6xl px-6"><p className="text-xs font-bold uppercase tracking-widest text-[#A78328]">Research preview</p><h2 className="mt-3 font-display text-3xl font-semibold text-[#0B1F3A]">Start with comparable evidence</h2><p className="mt-3 max-w-3xl text-slate-600">Previewing {Math.min(5,records.length)} of {total} strategies with published 1-year returns as of {PMS_PERFORMANCE_AS_OF}.</p><div className="mt-6 grid gap-3">{records.slice(0,5).map(row=><Link key={row.slug} href={`/pms-strategies/${row.slug}`} className="flex justify-between rounded-lg border bg-white p-4"><span className="font-semibold">{row.strategy_display_name}</span><span>{row.return_1y}</span></Link>)}</div><Link className="mt-6 inline-block font-semibold underline" href="/best-pms-in-india">Open the full PMS research universe</Link></div></section> }
